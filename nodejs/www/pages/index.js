var React = require('react');
var ReactDOM = require('react-dom');
var ComponentTest = require('../components/Test.jsx');

ReactDOM.render(React.createElement(ComponentTest), document.getElementById('testReact'));
